# Vercel Environment Variables
# Copy these to your Vercel dashboard under Settings > Environment Variables

# Authentication (Required for Login Gate)
VITE_AUTH_PASSWORD=your-dashboard-password
VITE_AUTH_STORAGE_KEY=your-storage-key

# Google Gemini API (Required for RAG System)
VITE_GEMINI_API_KEY=your-gemini-api-key

# Qdrant Vector Database (Required for Semantic Search)
VITE_QDRANT_URL=https://your-cluster.gcp.cloud.qdrant.io:6333
VITE_QDRANT_API_KEY=your-qdrant-api-key

# Note: Make sure all environment variables are available to:
# - Build time (VITE_ prefixed variables)
# - Runtime (API functions)
